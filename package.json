{
  "name": "glaze",
  "description": "",
  "repository": "git@github.com:davidepi/glaze.git",
  "author": "Davide Pizzolotto <davide.pizzolotto@gmail.com>",
  "private": true,
  "scripts": {
    "commitlint": "commitlint --from=$(git rev-list --max-parents=0 HEAD)",
    "markdownlint": "markdownlint . --ignore node_modules"
  },
  "devDependencies": {
    "@commitlint/cli": "^8.3.5",
    "@commitlint/config-conventional": "^8.3.4",
    "@commitlint/prompt-cli": "^8.3.5",
    "markdownlint-cli": "^0.23.1"
  }
}
